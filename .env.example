# Copy this file to .env and fill in values
#
# Required by our app (services/s3_service.py)
S3_BUCKET_NAME=

# Optional AWS region (if not already configured in your environment)
AWS_REGION=
# AWS_DEFAULT_REGION=

# Optional: Amazon Bedrock embeddings (used by services/document_text_service.py)
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0
BEDROCK_EMBEDDING_DIM=1024

# Optional: for LocalStack or custom S3-compatible endpoints
# S3_ENDPOINT_URL=http://localhost:4566
S3_ENDPOINT_URL=

# If you use two OpenSearch Serverless collections (search + vector), configure both endpoints:
OPENSEARCH_SEARCH_ENDPOINT=
OPENSEARCH_VECTOR_ENDPOINT=
# If not set, falls back to AWS_REGION/AWS_DEFAULT_REGION
OPENSEARCH_REGION=
# Optional: override SigV4 service name. If unset, inferred from endpoint.
# - Managed domains: es
# - Serverless collections: aoss
OPENSEARCH_SERVICE_NAME=
# Optional: request timeout
OPENSEARCH_TIMEOUT_SECONDS=30

# Optional: collection names (documentation only; not used by the app directly)
OPENSEARCH_SEARCH_COLLECTION_NAME=
OPENSEARCH_VECTOR_COLLECTION_NAME=

# Optional: index names used inside the collections
OPENSEARCH_SEARCH_INDEX_NAME=sagemaker-docs-search-index
OPENSEARCH_VECTOR_INDEX_NAME=sagemaker-docs-vectors-index

# AWS credentials (use ONE approach)
# Option A: explicit keys
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# Option B: profile-based auth (shared credentials file)
# AWS_PROFILE=default
AWS_PROFILE=
